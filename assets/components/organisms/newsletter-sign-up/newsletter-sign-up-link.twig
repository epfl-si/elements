{% if add_wrapper|default(true) %}
<div class="py-5">
{% endif %}
  <div class="container">
    <div class="row">
      <div class="col-md-10 offset-md-1 col-lg-8 offset-lg-2 border border-light p-4">
        <h3 class="mb-2">Inscrivez-vous à la newsletter!</h3>
        <div>Pour ne rien rater de l’actualité scientifique, inscrivez-vous et recevez un e-mail à chaque nouvelle publication</div>
        <a href="#" class="btn btn-primary mt-3">S'inscrire à la newsletter</a>
        </div>
    </div>
  </div>
{% if add_wrapper|default(true) %}
</div>
{% endif %}

