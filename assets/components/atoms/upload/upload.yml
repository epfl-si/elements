title: Upload
name: upload
notes: |
  If you want to *upload more than one file*, you can add the `multiple` on the file input.

  *It's recommanded to test this component in full render view after a good reload*
