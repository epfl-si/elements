name: popover
title: Popover
notes: |
  If you use the `.btn-circle` class on the popover toggle, the popover text should be also added in a `.sr-only` paragraph.

  See the [cover](/#/molecules/cover) component for an implementation inside a `<figcaption>`.
