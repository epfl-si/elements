<div class="overlay"></div>
<nav class="nav-main">
  <span class="nav-close" role="button" aria-description="Close secondary menu"></span>
  <div class="nav-wrapper">
    <div class="nav-container">
      <ul class="nav-menu">
        {% for i in 1..6 %}
          <li class="{{ i == 2 ? 'current_page_ancestor' : ''}}">
            <a data-page-id="{{ i == 2 ? 42 : 88 }}" href="">À propos</a>
            <a href="#" role="button" aria-hidden="true" class="nav-arrow">
              <div class="icon-container">
                {% include '@atoms/icon/icon.twig' with {icon: 'icon-chevron-right'} %}
              </div>
            </a>
            <ul>
              <li class="nav-back d-xl-none">
                <a href="/test">
                  {% include '@atoms/icon/icon.twig' with {icon: 'icon-arrow-left'} %}
                  Retour: À propos
                </a>
              </li>
              <li class="nav-home d-none d-xl-block">
                <a href="/test">
                  {% include '@atoms/icon/icon.twig' with {icon: 'icon-home'} %}
                  <span class="text-muted">&rsaquo;</span>
                  À propos
                </a>
              </li>
              {% for y in 1..3 %}
                <li class="{{ y == 2 and i == 2 ? 'current_page_parent' : ''}}">
                  <a data-page-id="77" href="/test">Stratégie de l’enseignement</a>
                  <a href="#" role="button" aria-hidden="true" class="nav-arrow">
                    <div class="icon-container">
                      {% include '@atoms/icon/icon.twig' with {icon: 'icon-chevron-right'} %}
                    </div>
                  </a>
                  <ul>
                    <li class="nav-back">
                      <a href="/test">
                      {% include '@atoms/icon/icon.twig' with {icon: 'icon-arrow-left'} %}Retour: Stratégie de l’enseignement</a>
                    </li>
                    <li>
                      <a  data-page-id="66" href="/test">Stratégie de l’enseignement</a>
                    </li>
                    <li class="{{ y == 2 and i == 2 ? 'current_page_item' : ''}}">
                      <a href="/test">Innovation dans l'enseignement</a>
                    </li>
                  </ul>
                </li>
              {% endfor %}
            </ul>
          </li>
        {% endfor %}
      </ul>
    </div>
  </div>
</nav>
