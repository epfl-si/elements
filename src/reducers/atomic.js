// import {
//   CANCEL_SEARCH,
// } from "../actions/index";

export default function atomicReducer(state = {}, action) {
  switch(action.type) {
    // case SEARCHED_BEERS_LOADING:
    //   return {
    //     ...state,
    //   };
    default: return state;
  }
}
